{
  "private": true,
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "diff": "lerna diff modules",
    "new-version": "lerna version --conventional-commits --yes",
    "publish": "lerna publish modules"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  },
  "name": "terraform-modules"
}
