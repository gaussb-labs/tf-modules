{
  "private": true,
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "diff": "lerna diff ",
    "new-version": "lerna version --conventional-commits --yes",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  },
  "name": "terraform-modules"
}
